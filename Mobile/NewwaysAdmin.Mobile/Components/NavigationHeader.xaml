<?xml version="1.0" encoding="utf-8" ?>
<ContentView x:Class="NewwaysAdmin.Mobile.Components.NavigationHeader"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid RowDefinitions="Auto,Auto">

        <!-- Row 0: Top bar (tappable to expand) -->
        <Grid Grid.Row="0" Padding="15,12" BackgroundColor="#512BD4">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnHeaderTapped" />
            </Grid.GestureRecognizers>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Page title -->
            <Label Grid.Column="0" 
                   x:Name="TitleLabel"
                   Text="NewwaysAdmin" 
                   FontSize="18" 
                   FontAttributes="Bold"
                   TextColor="White"
                   VerticalOptions="Center" />

            <!-- Connection dot -->
            <BoxView Grid.Column="1"
                     x:Name="ConnectionDot"
                     WidthRequest="12" 
                     HeightRequest="12" 
                     CornerRadius="6"
                     Color="Gray"
                     VerticalOptions="Center" />
        </Grid>

        <!-- Row 1: Expandable menu -->
        <Frame Grid.Row="1" 
               x:Name="MenuFrame"
               Padding="0" 
               Margin="0"
               CornerRadius="0"
               HasShadow="True"
               BackgroundColor="#6B46C1"
               BorderColor="Transparent"
               IsVisible="False">
            <StackLayout x:Name="MenuStack" Padding="15,12" Spacing="10">
                <!-- Menu items added dynamically based on permissions -->
            </StackLayout>
        </Frame>

    </Grid>

</ContentView>